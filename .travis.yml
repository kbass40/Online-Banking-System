language: python
python:
  - "3.6"
branches:
  # whitelist
  only:
    - travis_ci_test
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script: pytest
jobs:
  include:
    #- stage: "Microservices"
    -  script: test_Apple.py
    -  script: test_Facebook.py
    -  script: test_Google.py
    -  script: test_Oracle.py
    -  script: test_Ubisoft.py


